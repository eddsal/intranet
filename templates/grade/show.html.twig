{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Student</th>
                <td>{{ grade.user }}</td>
            </tr>
            <tr>
                <th>Grade</th>
                <td>{{ grade.grade }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ grade.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grade_administration_index') }}">back to list</a>

    <a href="{{ path('grade_administration_edit', {'id': grade.id}) }}">edit</a>

    {{ include('grade/_delete_form.html.twig') }}
{% endblock %}
